using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FastMonster : monsterAttributes 
{
    // Start is called before the first frame update
    void Start()
    {
        setSpeed(7f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
