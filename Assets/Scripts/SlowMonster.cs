using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SlowMonster : monsterAttributes 
{
    // Start is called before the first frame update
    void Start()
    {
        setSpeed(1.7f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
